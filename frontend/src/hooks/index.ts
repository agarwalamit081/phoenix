/**
 * Hooks index file
 * Export all custom hooks from this directory
 */

export { useVoiceInput } from "./useVoiceInput";
export type { VoiceState, VoiceTranscript, UseVoiceInputOptions, UseVoiceInputReturn } from "./useVoiceInput";
